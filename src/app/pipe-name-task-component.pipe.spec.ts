import { PipeNameTaskComponentPipe } from './pipe-name-task-component.pipe';

describe('PipeNameTaskComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNameTaskComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
