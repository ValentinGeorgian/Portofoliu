import { PipeProductsComponentPipe } from './pipe-products-component.pipe';

describe('PipeProductsComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProductsComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
