import { PipeProductTablePipe } from './pipe-product-table.pipe';

describe('PipeProductTablePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProductTablePipe();
    expect(pipe).toBeTruthy();
  });
});
