import { PipeTablePricePipe } from './pipe-table-price.pipe';

describe('PipeTablePricePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTablePricePipe();
    expect(pipe).toBeTruthy();
  });
});
